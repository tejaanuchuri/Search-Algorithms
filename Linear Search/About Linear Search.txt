Linear Search :

    1. A simple approach is to do a linear search, i.e  
    2. Start from the leftmost element of arr[] and one by one compare x with each element of arr[]
    3. If Key matches with an element, return the True.
    4. If Key doesn’t match with any of elements, return False.


Time Complexcity    :   o(n)
Space Complexcity   :   o(1)